<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tax Form - Total Income</title>
  <!-- Link CSS from /public -->
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="wrapper">
    <div class="card">
      <div class="card-header">
        <h1>Tax Form</h1>
        <p>Enter your income from two sources</p>
      </div>

      <form action="/calculate" method="post" class="form">
        <div class="form-group">
          <label for="income1">Income Source 1</label>
          <input id="income1" name="income1" type="number" placeholder="e.g. 5000" value="<%= inputs.income1 %>" required />
        </div>

        <div class="form-group">
          <label for="income2">Income Source 2</label>
          <input id="income2" name="income2" type="number" placeholder="e.g. 3000" value="<%= inputs.income2 %>" required />
        </div>

        <button type="submit">Calculate Total</button>
      </form>

      <% if (error) { %>
        <div class="error"><%= error %></div>
      <% } else if (result !== null) { %>
        <div class="result">Total Income: <strong>$<%= result %></strong></div>
      <% } %>
    </div>
  </div>
</body>
</html>
